<!DOCTYPE html>
<html>
<head>
<style>
h2 {text-align: center; background-color: green; border: 2px solid black; color: white}
button {data-inline="true";}
</style>
</head>
<body>
    <button class="btn btn-primary" onclick=root_path>American</button>
    <button class="btn btn-primary" onclick=root_path>Asian</button>
    <button class="btn btn-primary" onclick=root_path>Italian</button>
    <button class="btn btn-primary" onclick=root_path>Dessert</button>
    <button class="btn btn-primary" onclick=root_path>Other</button>
    
    <% title = ["Arby's", "Subway"] %>
    <% category = ["American", "American"] %>
    <% description = ["We have the meats", "We got the subs"] %>

    <p></p>
    <table>
        <thead>
            <tr>
                <th>Restaurant</th>
                <th>Category</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            <% title.each do |title| %>
                <tr>
                    <td><%= title %></td>
                    <td><%= category %></td>
                    <td><%= description %></td>
                </tr>
            <% end %>
        </tbody>
    </table>
</body>
</html>